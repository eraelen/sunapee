<% include userHeader %>
<% include navbar %>
<div class="center">
	<div id="leftContainer">
		<div id="searchResultCategory">
			<p>
				<a href="/searchT/<%= searchPhrase %>">Tweets</a><br>
				<font color="#45c7b1">--------------</font><br>
				<b>People</b><br>
			</p>
		</div>	
		<div id="trendHashtags">
			<p>THIS IS trending hash tags</p>
		</div>
	</div>
	
	<div id="rightContainer">		
		<div id="searchResults">
			<h3>Results for <font color="red"><%= searchPhrase %></font> :</h3>
			<% if (users !== null) { %>
				<% for (var i=0; i < users.length; i++) { %>
					<div class= "user">
						<%= users[i].name %> <a href="/<%= users[i].username %>/profile"> @<%= users[i].username %>
					</div>
				<% } %>
			<% } else { %>
				<p> No results found. </p>
			<% } %>
		</div>
	</div>
</div>

<% include footer %>